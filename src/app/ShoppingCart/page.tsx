import ShoppingCart from "@/screens/ShoppingCart";
import React from "react";

const page = () => {
    return (
        <div>
            <ShoppingCart />
        </div>
    );
};

export default page;
